# sorz

Simple operating system writen in Zig for the RISCV32 architecture, to compile and run the project you can use the command

```bash
zig build run
```

or to run with optimizations turn on

```bash
zig build run -Doptimize=ReleaseSafe
```
